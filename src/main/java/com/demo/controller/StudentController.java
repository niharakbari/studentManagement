package com.demo.controller;

import org.springframework.web.bind.annotation.RestController;

/**
 * REST controller placeholder.
 *
 * This project is now using classic Spring MVC with JSP
 * views only (see StudentControllerView). We keep this
 * empty controller so that any old REST URLs no longer
 * respond with JSON.
 */
@RestController
public class StudentController {
}